# 次回セッション用 指示書

このドキュメントは、次のセッションでスムーズに作業を再開するための指示書です。

---

## 📋 現在の進捗状況

### ✅ 完了したこと

#### Phase 1-10: バックエンド開発（完了）🎉

**詳細は後半のセクションを参照**
- ✅ Phase 1: 環境構築
- ✅ Phase 2: ドメイン層（Entity, Repository）
- ✅ Phase 3: インフラ層（Spring Security）
- ✅ Phase 4: アプリケーション層（DTO, Service）
- ✅ Phase 5: プレゼンテーション層（Controller, GlobalExceptionHandler）
- ✅ Phase 6: 外部連携（EmailService, GoogleCalendarService）
- ✅ Phase 7: バックエンドテスト・動作確認
- ✅ Phase 8: フロントエンド環境構築
- ✅ Phase 9: FigmaAI用プロンプト作成
- ✅ Phase 10: セッションベース認証の完全実装

#### フロントエンド Phase 1-5: 全ページ実装（完了）🎉

- ✅ Phase 1: 基礎コンポーネント実装
- ✅ Phase 2: LoginとSignupページ実装
- ✅ Phase 3: 予約枠一覧ページ実装
- ✅ Phase 4: ユーザーページ実装
- ✅ Phase 5: スタッフページ実装

#### 外部連携・認証機能（完了）🎉

**Google Calendar連携**
- ✅ OAuth2 Google Login実装
- ✅ Googleカレンダーイベント作成（予約時）
- ✅ Googleカレンダーイベント削除（キャンセル時）
- ✅ Viteプロキシ設定（cookieDomainRewrite）
- ✅ CORS設定（withCredentials対応）

**メール認証機能**
- ✅ 新規登録時のメール認証トークン発行
- ✅ 確認メール送信（MailHog連携）
- ✅ メール認証エンドポイント（リダイレクト対応）
- ✅ 未認証ユーザーのログインブロック
- ✅ フロントエンドでの認証成功/失敗メッセージ表示

#### ビジネスルール実装（完了）🎉

**予約作成時のチェック**
- ✅ 24時間前チェック：開始24時間前を切った予約は不可
- ✅ 定員超過チェック：満席の枠は予約不可
- ✅ 重複予約チェック：同一ユーザーの同一枠への重複予約防止

**キャンセル時のチェック**
- ✅ 24時間前チェック：開始24時間前を切ったキャンセルは不可
- ✅ ステータスチェック：既にキャンセル済みの予約はキャンセル不可
- ✅ 権限チェック：予約した本人のみキャンセル可能

**スロット管理のチェック**
- ✅ 定員減少制限：現在の予約数より少ない定員には変更不可

#### フロントエンドエラーハンドリング改善（完了）🎉

- ✅ SlotDetailPage.tsx：バックエンドエラーメッセージ表示対応
- ✅ MyReservationsPage.tsx：バックエンドエラーメッセージ表示対応
- ✅ StaffSlotsPage.tsx：バックエンドエラーメッセージ表示対応（元から対応済み）

---

## 🧪 統合テスト進捗

### テストケース一覧（計60件）✅ 全て完了

| カテゴリ | テストケース | 件数 | 状況 |
|---------|------------|------|------|
| 認証テスト | TC-101〜TC-108 | 8件 | ✅ 完了 |
| 予約フローテスト | TC-201〜TC-212 | 12件 | ✅ 完了 |
| スタッフ機能テスト | TC-301〜TC-321 | 21件 | ✅ 完了 |
| Google Calendar連携テスト | TC-401〜TC-408 | 8件 | ✅ 完了 |
| メール認証テスト | TC-501〜TC-505 | 5件 | ✅ 完了 |
| ビジネスルールテスト | TC-601〜TC-606 | 6件 | ✅ 完了 |

### ビジネスルールテスト詳細（TC-601〜TC-606）

| ID | テスト内容 | 期待されるエラーメッセージ | 状況 |
|----|-----------|------------------------|------|
| TC-601 | 24時間前を切った予約作成 | 「予約受付は開始24時間前までです」 | ✅ 完了 |
| TC-602 | 定員超過の予約作成 | 「満席のため予約できません」 | ✅ 完了 |
| TC-603 | 重複予約の作成 | 「既にこの枠を予約済みです」 | ✅ 完了 |
| TC-604 | 24時間前を切ったキャンセル | 「開始24時間前を過ぎているためキャンセルできません」 | ✅ 完了 |
| TC-605 | キャンセル済み予約の再キャンセル | 「この予約は既にキャンセルされています」 | ✅ 完了 |
| TC-606 | 予約数未満への定員減少 | 「現在の予約数（N件）より少ない定員には変更できません」 | ✅ 完了 |

---

## 🎯 次のステップ

### プロジェクト完了！🎉

**全ての開発・テストが完了しました！**

#### 完了した内容
- ✅ バックエンド開発（Phase 1-10）
- ✅ フロントエンド開発（Phase 1-5）
- ✅ 外部連携（Google Calendar、メール認証）
- ✅ ビジネスルール実装
- ✅ 統合テスト（60件全て完了）

#### 今後の拡張案（オプション）
1. **本番環境へのデプロイ**
   - AWS/GCP/Herokuなどへのデプロイ
   - 本番用のGoogle OAuth2設定
   - 本番用のメールサーバー設定

2. **機能拡張**
   - 予約リマインダーメール送信
   - 管理者向けダッシュボード強化
   - レポート・分析機能

3. **テスト強化**
   - E2Eテスト（Playwright/Cypress）
   - ユニットテスト追加
   - 負荷テスト

---

## 📝 最新の実装変更点

### バックエンド

#### ReservationService.java
```java
// 24時間前チェック
LocalDateTime now = LocalDateTime.now();
LocalDateTime deadline = slot.getStartTime().minusHours(24);
if (now.isAfter(deadline)) {
    throw new InvalidRequestException("予約受付は開始24時間前までです");
}

// 残席チェック
long reservedCount = reservationRepository.countBySlotIdAndStatus(
    slot.getId(), ReservationStatus.RESERVED);
if (reservedCount >= slot.getCapacity()) {
    throw new InvalidRequestException("満席のため予約できません");
}

// 重複予約チェック
boolean alreadyReserved = reservationRepository.existsBySlotIdAndUserIdAndStatus(
    slot.getId(), userId, ReservationStatus.RESERVED);
if (alreadyReserved) {
    throw new InvalidRequestException("既にこの枠を予約済みです");
}
```

#### SlotService.java
```java
// 定員減少制限
long reservedCount = reservationRepository.countBySlotIdAndStatus(
    slot.getId(), ReservationStatus.RESERVED);
if (request.getCapacity() < reservedCount) {
    throw new InvalidRequestException(
        "現在の予約数（" + reservedCount + "件）より少ない定員には変更できません");
}
```

#### ReservationRepository.java
```java
// 重複予約チェック用メソッド追加
boolean existsBySlotIdAndUserIdAndStatus(Long slotId, Long userId, ReservationStatus status);
```

### フロントエンド

#### SlotDetailPage.tsx（予約作成エラーハンドリング）
```typescript
} catch (error: unknown) {
    console.error("予約の作成に失敗しました", error);
    let errorMessage = "予約の作成に失敗しました。もう一度お試しください";
    if (error && typeof error === 'object' && 'response' in error) {
        const axiosError = error as { response?: { data?: { message?: string } } };
        if (axiosError.response?.data?.message) {
            errorMessage = axiosError.response.data.message;
        }
    }
    alert(errorMessage);
}
```

#### MyReservationsPage.tsx（キャンセルエラーハンドリング）
```typescript
} catch (error) {
    console.error("予約のキャンセルに失敗しました", error);
    if (axios.isAxiosError(error)) {
        const errorMessage = error.response?.data?.message ||
            "予約のキャンセルに失敗しました。もう一度お試しください。";
        alert(errorMessage);
    } else {
        alert("予期しないエラーが発生しました");
    }
}
```

---

## 📂 現在のファイル構成（主要ファイルのみ）

### バックエンド

```
reservation/src/main/java/com/example/reservation/
├── controller/
│   ├── AuthController.java ✅（メール認証リダイレクト対応）
│   ├── ReservationController.java ✅（/my エンドポイント追加）
│   └── ...
├── service/
│   ├── AuthService.java ✅（メール認証チェック追加）
│   ├── ReservationService.java ✅（ビジネスルール追加）
│   ├── SlotService.java ✅（定員減少制限追加）
│   ├── EmailService.java ✅（設定値外部化）
│   └── GoogleCalendarService.java ✅
├── repository/
│   └── ReservationRepository.java ✅（existsBySlotIdAndUserIdAndStatus追加）
└── security/
    └── SecurityConfig.java ✅（CORS設定追加）
```

### フロントエンド

```
reservation-frontend/src/
├── api/
│   ├── client.ts ✅（withCredentials: true）
│   ├── authApi.ts ✅（相対URL使用）
│   └── reservationApi.ts ✅（/my エンドポイント使用）
├── pages/
│   ├── LoginPage.tsx ✅（認証成功/失敗メッセージ表示）
│   ├── SlotDetailPage.tsx ✅（エラーメッセージ表示改善）
│   ├── MyReservationsPage.tsx ✅（エラーメッセージ表示改善）
│   └── StaffSlotsPage.tsx ✅（エラーメッセージ表示対応済み）
└── ...
```

### 設定ファイル

```
reservation/src/main/resources/
└── application.yml ✅
    - Google OAuth2クライアント設定
    - app.base-url設定
    - app.mail.from設定

reservation-frontend/
└── vite.config.ts ✅
    - /api, /oauth2, /login/oauth2 プロキシ設定
    - cookieDomainRewrite設定
```

---

## 🚀 次回セッションの開始方法

### 1. セッション開始時の宣言

次のセッションで、以下のように伝えてください:

```
予約システムの開発が全て完了しました！🎉

完了した内容：
- バックエンド開発（Phase 1-10）
- フロントエンド開発（Phase 1-5）
- 外部連携（Google Calendar、メール認証）
- ビジネスルール実装（6項目）
- 統合テスト（60件全て完了）

今後は以下のいずれかを進めたいです：
- 本番環境へのデプロイ
- 追加機能の実装
- その他（具体的に記載）
```

---

### 2. 開発サーバー起動確認

#### バックエンド（Spring Boot）

```bash
# 1. Dockerコンテナを起動
cd /Users/yanagitanaoki/Documents/ReservationSystem/reservation
docker compose up -d
docker compose ps

# 2. バックエンド起動（別ターミナル）
./gradlew bootRun
```

**期待される動作:**
- `HikariPool-1 - Start completed.`（接続成功）
- `Tomcat started on port 8080`（起動成功）
- Swagger UI: http://localhost:8080/swagger-ui.html

#### フロントエンド（React + Vite）

```bash
# 3. フロントエンド起動（別ターミナル）
cd /Users/yanagitanaoki/Documents/ReservationSystem/reservation-frontend
npm run dev
```

**期待される動作:**
- `VITE v7.3.0  ready in XXX ms`
- ` ➜  Local:   http://localhost:5173/`
- エラーなしで起動

---

### 3. テスト実施方法

#### TC-601: 24時間前を切った予約作成
1. スタッフでログイン
2. 24時間以内に開始するスロットを作成
3. 一般ユーザーでログイン
4. そのスロットの予約を試みる
5. 「予約受付は開始24時間前までです」が表示されることを確認

#### TC-602: 定員超過の予約作成
1. スタッフでログイン
2. 定員1のスロットを作成
3. ユーザーAでログインし、そのスロットを予約
4. ユーザーBでログイン
5. 同じスロットの予約を試みる
6. 「満席のため予約できません」が表示されることを確認

#### TC-603: 重複予約の作成
1. ユーザーAでログイン
2. スロットを予約
3. 同じスロットの予約を再度試みる
4. 「既にこの枠を予約済みです」が表示されることを確認

#### TC-604: 24時間前を切ったキャンセル
1. 24時間以内に開始する予約を持つユーザーでログイン
2. その予約のキャンセルを試みる
3. 「開始24時間前を過ぎているためキャンセルできません」が表示されることを確認

#### TC-605: キャンセル済み予約の再キャンセル
1. APIで直接キャンセル済み予約のキャンセルを試みる（UIでは表示されないため）
2. 「この予約は既にキャンセルされています」が返ることを確認

#### TC-606: 予約数未満への定員減少
1. スタッフでログイン
2. 定員3で2件の予約があるスロットを選択
3. 定員を1に減らそうとする
4. 「現在の予約数（2件）より少ない定員には変更できません」が表示されることを確認

---

## 📝 次回セッション開始時のチェックリスト

- [ ] Mac上のPostgreSQLが停止していることを確認（`lsof -i :5432`）
- [ ] Dockerコンテナが起動していることを確認（`docker compose ps`）
- [ ] バックエンドが起動していることを確認（http://localhost:8080/swagger-ui.html）
- [ ] フロントエンドが起動していることを確認（http://localhost:5173/）
- [ ] テストケース一覧を確認（テストケース一覧_統合テスト.md）

---

## 🔑 重要な認証情報（開発用）

### テストユーザー
- **一般ユーザー**: test@example.com / password123
- **スタッフ**: staff@example.com / password123

### Google OAuth2（本番用に設定済み）
- Client ID: 436900733622-qra0l4lhlffd3j5p41mej0vmvvb1bfhj.apps.googleusercontent.com
- リダイレクトURI: http://localhost:5173/login/oauth2/code/google

---

この指示書があれば、次のセッションでもスムーズに作業を再開できます！

**予約システム開発、完了おめでとうございます！** 🎉🎉🎉

全60件の統合テストが完了し、システムは本番運用可能な状態です！
