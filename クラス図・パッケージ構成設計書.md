# クラス図・パッケージ構成設計書

**プロジェクト名**: ミニ予約システム（定員あり）
**バージョン**: 1.0 (MVP)
**作成日**: 2025-12-13
**対象フェーズ**: 詳細設計

---

## 目次

1. [パッケージ構成](#パッケージ構成)
2. [クラス一覧](#クラス一覧)
3. [Entity（エンティティ）](#entityエンティティ)
4. [Repository（リポジトリ）](#repositoryリポジトリ)
5. [Service（サービス）](#serviceサービス)
6. [Controller（コントローラ）](#controllerコントローラ)
7. [DTO（データ転送オブジェクト）](#dtoデータ転送オブジェクト)
8. [Exception（例外）](#exception例外)
9. [Configuration（設定）](#configuration設定)
10. [Security（セキュリティ）](#securityセキュリティ)
11. [クラス図](#クラス図)

---

## パッケージ構成

```
com.example.reservation
│
├── entity/                     # ドメイン層 - エンティティ
│   ├── User.java
│   ├── ServiceMenu.java
│   ├── Slot.java
│   ├── Reservation.java
│   ├── EmailVerificationToken.java
│   └── enums/
│       ├── UserRole.java
│       ├── SlotStatus.java
│       └── ReservationStatus.java
│
├── repository/                 # ドメイン層 - リポジトリ
│   ├── UserRepository.java
│   ├── ServiceMenuRepository.java
│   ├── SlotRepository.java
│   ├── ReservationRepository.java
│   └── EmailVerificationTokenRepository.java
│
├── service/                    # アプリケーション層 - サービス
│   ├── AuthService.java
│   ├── UserService.java
│   ├── ServiceMenuService.java
│   ├── SlotService.java
│   ├── ReservationService.java
│   ├── EmailService.java
│   └── GoogleCalendarService.java
│
├── controller/                 # プレゼンテーション層 - コントローラ
│   ├── AuthController.java
│   ├── UserController.java
│   ├── ServiceMenuController.java
│   ├── SlotController.java
│   ├── StaffSlotController.java
│   ├── ReservationController.java
│   ├── StaffReservationController.java
│   └── advice/
│       └── GlobalExceptionHandler.java
│
├── dto/                        # データ転送オブジェクト
│   ├── request/
│   │   ├── SignupRequest.java
│   │   ├── LoginRequest.java
│   │   ├── CreateServiceMenuRequest.java
│   │   ├── CreateSlotRequest.java
│   │   ├── UpdateSlotRequest.java
│   │   └── CreateReservationRequest.java
│   └── response/
│       ├── UserResponse.java
│       ├── ServiceMenuResponse.java
│       ├── SlotResponse.java
│       ├── ReservationResponse.java
│       └── ErrorResponse.java
│
├── exception/                  # カスタム例外
│   ├── BusinessException.java
│   ├── ResourceNotFoundException.java
│   ├── DuplicateReservationException.java
│   ├── SlotFullException.java
│   ├── CancelDeadlinePassedException.java
│   ├── InvalidTokenException.java
│   └── UnauthorizedException.java
│
├── config/                     # インフラ層 - 設定
│   ├── SecurityConfig.java
│   ├── CorsConfig.java
│   ├── MailConfig.java
│   ├── SwaggerConfig.java
│   └── GoogleApiConfig.java
│
├── security/                   # インフラ層 - セキュリティ
│   ├── CustomUserDetails.java
│   ├── CustomUserDetailsService.java
│   ├── OAuth2SuccessHandler.java
│   └── OAuth2UserService.java
│
├── external/                   # インフラ層 - 外部API
│   └── GoogleCalendarClient.java
│
└── util/                       # ユーティリティ
    └── DateTimeUtil.java
```

---

## クラス一覧

### Entity（5クラス + 3 Enum）

| クラス名 | 説明 |
|---------|------|
| User | ユーザー情報 |
| ServiceMenu | サービスメニュー |
| Slot | 予約枠 |
| Reservation | 予約 |
| EmailVerificationToken | メール確認トークン |
| UserRole (Enum) | ユーザーロール（USER, STAFF, ADMIN） |
| SlotStatus (Enum) | 枠ステータス（OPEN, CLOSED, CANCELLED） |
| ReservationStatus (Enum) | 予約ステータス（RESERVED, CANCELLED） |

### Repository（5インターフェース）

| インターフェース名 | 説明 |
|------------------|------|
| UserRepository | ユーザーリポジトリ |
| ServiceMenuRepository | メニューリポジトリ |
| SlotRepository | 予約枠リポジトリ |
| ReservationRepository | 予約リポジトリ |
| EmailVerificationTokenRepository | トークンリポジトリ |

### Service（7クラス）

| クラス名 | 説明 |
|---------|------|
| AuthService | 認証サービス |
| UserService | ユーザーサービス |
| ServiceMenuService | メニューサービス |
| SlotService | 予約枠サービス |
| ReservationService | 予約サービス |
| EmailService | メール送信サービス |
| GoogleCalendarService | Googleカレンダー連携サービス |

### Controller（7クラス + 1ハンドラ）

| クラス名 | 説明 |
|---------|------|
| AuthController | 認証API |
| UserController | ユーザーAPI |
| ServiceMenuController | メニューAPI |
| SlotController | 予約枠API（公開） |
| StaffSlotController | 予約枠API（STAFF） |
| ReservationController | 予約API |
| StaffReservationController | 全予約API（STAFF） |
| GlobalExceptionHandler | グローバル例外ハンドラ |

---

## Entity（エンティティ）

### User.java

```java
package com.example.reservation.entity;

import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;

@Entity
@Table(name = "users")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class User {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(unique = true, length = 255)
    private String googleSub;

    @Column(nullable = false, unique = true, length = 255)
    private String email;

    @Column(length = 255)
    private String password;

    @Column(nullable = false, length = 100)
    private String name;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false, length = 20)
    private UserRole role;

    @Column(nullable = false)
    private Boolean isEmailVerified = false;

    @Column(columnDefinition = "TEXT")
    private String googleAccessToken;

    @Column(columnDefinition = "TEXT")
    private String googleRefreshToken;

    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;

    // ビジネスメソッド
    public boolean isGoogleUser() {
        return googleSub != null;
    }

    public boolean hasCalendarAccess() {
        return googleAccessToken != null && googleRefreshToken != null;
    }
}
```

---

### UserRole.java (Enum)

```java
package com.example.reservation.entity.enums;

public enum UserRole {
    USER,
    STAFF,
    ADMIN
}
```

---

### ServiceMenu.java

```java
package com.example.reservation.entity;

import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;

@Entity
@Table(name = "service_menus")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class ServiceMenu {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false, length = 100)
    private String name;

    @Column(nullable = false)
    private Integer durationMinutes;

    @Column(nullable = false)
    private Boolean isActive = true;

    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;
}
```

---

### Slot.java

```java
package com.example.reservation.entity;

import com.example.reservation.entity.enums.SlotStatus;
import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;

@Entity
@Table(name = "slots")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Slot {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "menu_id", nullable = false)
    private ServiceMenu menu;

    @Column(nullable = false)
    private LocalDateTime startAt;

    @Column(nullable = false)
    private LocalDateTime endAt;

    @Column(nullable = false)
    private Integer capacity;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false, length = 20)
    private SlotStatus status = SlotStatus.OPEN;

    @Version
    @Column(nullable = false)
    private Long version = 0L;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "created_by", nullable = false)
    private User createdBy;

    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;

    // ビジネスメソッド
    public boolean isOpen() {
        return status == SlotStatus.OPEN;
    }

    public boolean canReserve(LocalDateTime now) {
        return isOpen() && startAt.isAfter(now.plusHours(24));
    }
}
```

---

### SlotStatus.java (Enum)

```java
package com.example.reservation.entity.enums;

public enum SlotStatus {
    OPEN,      // 予約受付中
    CLOSED,    // 予約受付停止
    CANCELLED  // 枠キャンセル
}
```

---

### Reservation.java

```java
package com.example.reservation.entity;

import com.example.reservation.entity.enums.ReservationStatus;
import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;
import org.hibernate.annotations.UpdateTimestamp;

import java.time.LocalDateTime;

@Entity
@Table(name = "reservations",
    uniqueConstraints = {
        @UniqueConstraint(
            name = "uk_slot_user_active",
            columnNames = {"slot_id", "user_id"}
        )
    }
)
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Reservation {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "slot_id", nullable = false)
    private Slot slot;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false, length = 20)
    private ReservationStatus status = ReservationStatus.RESERVED;

    @Column(length = 255)
    private String googleCalendarEventId;

    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @UpdateTimestamp
    @Column(nullable = false)
    private LocalDateTime updatedAt;

    // ビジネスメソッド
    public boolean canCancel(LocalDateTime now) {
        return status == ReservationStatus.RESERVED
            && slot.getStartAt().isAfter(now.plusHours(24));
    }

    public boolean hasCalendarEvent() {
        return googleCalendarEventId != null;
    }
}
```

**備考**: UNIQUE制約は部分インデックスで実装するため、アプリケーション層でもチェックが必要

---

### ReservationStatus.java (Enum)

```java
package com.example.reservation.entity.enums;

public enum ReservationStatus {
    RESERVED,   // 予約中
    CANCELLED   // キャンセル済み
}
```

---

### EmailVerificationToken.java

```java
package com.example.reservation.entity;

import jakarta.persistence.*;
import lombok.*;
import org.hibernate.annotations.CreationTimestamp;

import java.time.LocalDateTime;
import java.util.UUID;

@Entity
@Table(name = "email_verification_tokens")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class EmailVerificationToken {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;

    @Column(nullable = false, unique = true, length = 255)
    private String token;

    @Column(nullable = false)
    private LocalDateTime expiresAt;

    @Column
    private LocalDateTime usedAt;

    @CreationTimestamp
    @Column(nullable = false, updatable = false)
    private LocalDateTime createdAt;

    // ビジネスメソッド
    public static EmailVerificationToken create(User user) {
        return EmailVerificationToken.builder()
            .user(user)
            .token(UUID.randomUUID().toString())
            .expiresAt(LocalDateTime.now().plusHours(24))
            .build();
    }

    public boolean isExpired() {
        return LocalDateTime.now().isAfter(expiresAt);
    }

    public boolean isUsed() {
        return usedAt != null;
    }

    public void markAsUsed() {
        this.usedAt = LocalDateTime.now();
    }
}
```

---

## Repository（リポジトリ）

### UserRepository.java

```java
package com.example.reservation.repository;

import com.example.reservation.entity.User;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

@Repository
public interface UserRepository extends JpaRepository<User, Long> {

    Optional<User> findByEmail(String email);

    Optional<User> findByGoogleSub(String googleSub);

    boolean existsByEmail(String email);
}
```

---

### ServiceMenuRepository.java

```java
package com.example.reservation.repository;

import com.example.reservation.entity.ServiceMenu;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.List;

@Repository
public interface ServiceMenuRepository extends JpaRepository<ServiceMenu, Long> {

    List<ServiceMenu> findByIsActiveTrue();
}
```

---

### SlotRepository.java

```java
package com.example.reservation.repository;

import com.example.reservation.entity.Slot;
import com.example.reservation.entity.enums.SlotStatus;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.time.LocalDateTime;
import java.util.List;

@Repository
public interface SlotRepository extends JpaRepository<Slot, Long> {

    // 公開API用：OPEN, 残席あり, 未来の枠のみ
    @Query("SELECT s FROM Slot s " +
           "WHERE s.status = :status " +
           "AND s.startAt > :now " +
           "AND (:from IS NULL OR s.startAt >= :from) " +
           "AND (:to IS NULL OR s.startAt <= :to) " +
           "AND (:menuId IS NULL OR s.menu.id = :menuId) " +
           "ORDER BY s.startAt ASC")
    List<Slot> findPublicSlots(
        @Param("status") SlotStatus status,
        @Param("now") LocalDateTime now,
        @Param("from") LocalDateTime from,
        @Param("to") LocalDateTime to,
        @Param("menuId") Long menuId
    );

    // STAFF用：全枠取得
    @Query("SELECT s FROM Slot s " +
           "WHERE (:from IS NULL OR s.startAt >= :from) " +
           "AND (:to IS NULL OR s.startAt <= :to) " +
           "AND (:status IS NULL OR s.status = :status) " +
           "AND (:menuId IS NULL OR s.menu.id = :menuId) " +
           "ORDER BY s.startAt ASC")
    List<Slot> findAllSlots(
        @Param("from") LocalDateTime from,
        @Param("to") LocalDateTime to,
        @Param("status") SlotStatus status,
        @Param("menuId") Long menuId
    );
}
```

---

### ReservationRepository.java

```java
package com.example.reservation.repository;

import com.example.reservation.entity.Reservation;
import com.example.reservation.entity.enums.ReservationStatus;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.time.LocalDateTime;
import java.util.List;

@Repository
public interface ReservationRepository extends JpaRepository<Reservation, Long> {

    // ユーザーの予約一覧（ステータス指定）
    List<Reservation> findByUserIdAndStatusOrderBySlotStartAtAsc(Long userId, ReservationStatus status);

    // ユーザーの全予約履歴
    List<Reservation> findByUserIdOrderByCreatedAtDesc(Long userId);

    // 枠の予約数カウント
    Long countBySlotIdAndStatus(Long slotId, ReservationStatus status);

    // 重複予約チェック
    boolean existsBySlotIdAndUserIdAndStatus(Long slotId, Long userId, ReservationStatus status);

    // STAFF用：全予約取得
    @Query("SELECT r FROM Reservation r " +
           "JOIN FETCH r.slot s " +
           "JOIN FETCH r.user u " +
           "WHERE (:from IS NULL OR s.startAt >= :from) " +
           "AND (:to IS NULL OR s.startAt <= :to) " +
           "AND (:status IS NULL OR r.status = :status) " +
           "AND (:userName IS NULL OR u.name LIKE %:userName%) " +
           "ORDER BY s.startAt ASC")
    List<Reservation> findAllReservations(
        @Param("from") LocalDateTime from,
        @Param("to") LocalDateTime to,
        @Param("status") ReservationStatus status,
        @Param("userName") String userName
    );
}
```

---

### EmailVerificationTokenRepository.java

```java
package com.example.reservation.repository;

import com.example.reservation.entity.EmailVerificationToken;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.time.LocalDateTime;
import java.util.Optional;

@Repository
public interface EmailVerificationTokenRepository extends JpaRepository<EmailVerificationToken, Long> {

    Optional<EmailVerificationToken> findByToken(String token);

    void deleteByExpiresAtBefore(LocalDateTime dateTime);
}
```

---

## Service（サービス）

主要なサービスクラスのシグネチャのみ記載します。

### ReservationService.java

```java
package com.example.reservation.service;

import com.example.reservation.dto.response.ReservationResponse;
import com.example.reservation.entity.Reservation;

import java.util.List;

public interface ReservationService {

    /**
     * 予約作成（楽観ロック + Google Calendar連携）
     */
    ReservationResponse createReservation(Long slotId, Long userId);

    /**
     * 自分の予約一覧取得（進行中のみ）
     */
    List<ReservationResponse> getMyReservations(Long userId);

    /**
     * 予約履歴取得（全ステータス）
     */
    List<ReservationResponse> getReservationHistory(Long userId);

    /**
     * 予約キャンセル（Google Calendar連携）
     */
    ReservationResponse cancelReservation(Long reservationId, Long userId);
}
```

---

## Controller（コントローラ）

### ReservationController.java

```java
package com.example.reservation.controller;

import com.example.reservation.dto.request.CreateReservationRequest;
import com.example.reservation.dto.response.ReservationResponse;
import com.example.reservation.security.CustomUserDetails;
import com.example.reservation.service.ReservationService;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.validation.Valid;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@Tag(name = "予約API", description = "ユーザーの予約管理")
@RestController
@RequestMapping("/api/reservations")
@RequiredArgsConstructor
public class ReservationController {

    private final ReservationService reservationService;

    @Operation(summary = "予約作成", description = "予約枠に対して予約を作成します")
    @PreAuthorize("hasRole('USER')")
    @PostMapping
    public ResponseEntity<ReservationResponse> createReservation(
        @Valid @RequestBody CreateReservationRequest request,
        @AuthenticationPrincipal CustomUserDetails userDetails
    ) {
        ReservationResponse response = reservationService.createReservation(
            request.getSlotId(),
            userDetails.getUserId()
        );
        return ResponseEntity.status(HttpStatus.CREATED).body(response);
    }

    @Operation(summary = "自分の予約一覧")
    @PreAuthorize("hasRole('USER')")
    @GetMapping("/my")
    public ResponseEntity<List<ReservationResponse>> getMyReservations(
        @AuthenticationPrincipal CustomUserDetails userDetails
    ) {
        List<ReservationResponse> reservations = reservationService.getMyReservations(
            userDetails.getUserId()
        );
        return ResponseEntity.ok(reservations);
    }

    @Operation(summary = "予約履歴取得")
    @PreAuthorize("hasRole('USER')")
    @GetMapping("/history")
    public ResponseEntity<List<ReservationResponse>> getReservationHistory(
        @AuthenticationPrincipal CustomUserDetails userDetails
    ) {
        List<ReservationResponse> history = reservationService.getReservationHistory(
            userDetails.getUserId()
        );
        return ResponseEntity.ok(history);
    }

    @Operation(summary = "予約キャンセル")
    @PreAuthorize("hasRole('USER')")
    @PatchMapping("/{reservationId}/cancel")
    public ResponseEntity<ReservationResponse> cancelReservation(
        @PathVariable Long reservationId,
        @AuthenticationPrincipal CustomUserDetails userDetails
    ) {
        ReservationResponse response = reservationService.cancelReservation(
            reservationId,
            userDetails.getUserId()
        );
        return ResponseEntity.ok(response);
    }
}
```

---

## DTO（データ転送オブジェクト）

### CreateReservationRequest.java

```java
package com.example.reservation.dto.request;

import jakarta.validation.constraints.NotNull;
import lombok.Data;

@Data
public class CreateReservationRequest {

    @NotNull(message = "予約枠IDは必須です")
    private Long slotId;
}
```

---

### ReservationResponse.java

```java
package com.example.reservation.dto.response;

import com.example.reservation.entity.Reservation;
import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReservationResponse {

    private Long id;
    private Long slotId;
    private String menuName;

    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ssXXX")
    private LocalDateTime slotStartAt;

    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ssXXX")
    private LocalDateTime slotEndAt;

    private String status;
    private String googleCalendarEventId;
    private Boolean canCancel;

    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ssXXX")
    private LocalDateTime createdAt;

    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ssXXX")
    private LocalDateTime updatedAt;

    // エンティティからDTOへの変換
    public static ReservationResponse from(Reservation reservation) {
        return ReservationResponse.builder()
            .id(reservation.getId())
            .slotId(reservation.getSlot().getId())
            .menuName(reservation.getSlot().getMenu().getName())
            .slotStartAt(reservation.getSlot().getStartAt())
            .slotEndAt(reservation.getSlot().getEndAt())
            .status(reservation.getStatus().name())
            .googleCalendarEventId(reservation.getGoogleCalendarEventId())
            .canCancel(reservation.canCancel(LocalDateTime.now()))
            .createdAt(reservation.getCreatedAt())
            .updatedAt(reservation.getUpdatedAt())
            .build();
    }
}
```

---

## Exception（例外）

### BusinessException.java（基底クラス）

```java
package com.example.reservation.exception;

public class BusinessException extends RuntimeException {

    public BusinessException(String message) {
        super(message);
    }

    public BusinessException(String message, Throwable cause) {
        super(message, cause);
    }
}
```

---

### SlotFullException.java

```java
package com.example.reservation.exception;

public class SlotFullException extends BusinessException {

    public SlotFullException() {
        super("満席になりました");
    }
}
```

---

### DuplicateReservationException.java

```java
package com.example.reservation.exception;

public class DuplicateReservationException extends BusinessException {

    public DuplicateReservationException() {
        super("既に予約済みです");
    }
}
```

---

## Configuration（設定）

主要な設定クラスのシグネチャのみ記載します。

### SecurityConfig.java

```java
package com.example.reservation.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.web.SecurityFilterChain;

@Configuration
@EnableMethodSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        // Spring Security設定
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder(10);
    }
}
```

---

## クラス図

### 主要クラスの関係図

```
┌──────────────────┐
│   Controller     │
│  (REST API)      │
└────────┬─────────┘
         │
         │ uses
         ▼
┌──────────────────┐
│    Service       │
│ (ビジネスロジック)│
└────────┬─────────┘
         │
         │ uses
         ▼
┌──────────────────┐        ┌──────────────────┐
│   Repository     │◄───────│     Entity       │
│  (データアクセス)  │        │  (ドメインモデル)  │
└──────────────────┘        └──────────────────┘
```

---

## 変更履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|----------|
| 1.0 | 2025-12-13 | 初版作成（クラス図・パッケージ構成確定） |
