# フロントエンド開発 用語集

フロントエンド開発でよく使われる用語を、初心者向けにわかりやすく解説します。

---

## 📋 目次

1. [仮想DOM](#仮想dom)
2. [Vite（ヴィート）](#viteヴィート)
3. [ESModules](#esmodules)
4. [Promise / Promiseベース](#promise--promiseベース)
5. [インターセプター機能](#インターセプター機能)
6. [JSONパース](#jsonパース)
7. [シングルページアプリケーション（SPA）](#シングルページアプリケーションspa)
8. [package-lock.json](#package-lockjson)
9. [CI/CD](#cicd)
10. [カスタムフック](#カスタムフック)
11. [ユーティリティ関数](#ユーティリティ関数)
12. [プロキシ機能](#プロキシ機能)
13. [バンドル](#バンドル)
14. [Nginx](#nginx)

---

## 仮想DOM

### 簡単に言うと

**DOMのコピーをメモリ上に持っておいて、効率的に画面を更新する仕組み**

### 詳しく説明

**DOM（Document Object Model）とは？**
- HTMLをJavaScriptで操作できる形にしたもの
- ブラウザがHTMLを読み込んで作る「画面の構造」

**問題点：**
```javascript
// 普通のDOM操作（遅い）
document.getElementById('title').textContent = '新しいタイトル';
document.getElementById('price').textContent = '3000円';
document.getElementById('description').textContent = '...';
// → 3回も画面を書き換える（重い）
```

**仮想DOMの仕組み：**

```
1. 変更前の仮想DOM（メモリ上）
   ┌─────────────┐
   │  タイトル   │
   │  価格: 2000 │
   │  説明: ...  │
   └─────────────┘

2. 変更後の仮想DOM（メモリ上）
   ┌─────────────┐
   │  タイトル   │
   │  価格: 3000 │ ← ここだけ変更
   │  説明: ...  │
   └─────────────┘

3. 差分を計算（Reactが自動で）
   → 価格だけ変更されている

4. 実際のDOM更新（1回だけ）
   → 価格の部分だけ書き換える
```

**メリット：**
- 画面の更新が高速
- 必要な部分だけ更新
- 開発者は細かい最適化を考えなくて良い

**レストランの例え：**
```
通常のDOM操作:
→ メニューが1つ変わるたびに、メニュー表全体を作り直す

仮想DOM:
→ 変更箇所だけシールを貼り替える
```

---

## Vite（ヴィート）

### 簡単に言うと

**超高速な開発サーバーとビルドツール（フランス語で「速い」という意味）**

### 詳しく説明

**何をするツール？**
1. 開発サーバーを起動（コードを書く → すぐ反映）
2. 本番用にコードをビルド（圧縮・最適化）

**なぜ速い？**

**従来のツール（Webpack）：**
```
起動時
├─ 全ファイルを読み込む
├─ 全ファイルをバンドル（まとめる）
└─ 起動完了（30秒かかる）

変更時
├─ 関連ファイルを再バンドル
└─ 画面更新（2-3秒）
```

**Vite：**
```
起動時
├─ 必要最小限のファイルだけ読む
└─ 起動完了（1秒！）

変更時
├─ 変更ファイルだけ処理
└─ 画面更新（ほぼ瞬時）
```

**新幹線の例え：**
```
Webpack: 各駅停車（全部の駅に止まる）
Vite: のぞみ（主要駅だけ）
```

**実際の速度比較：**
| ツール | 起動時間 | 変更時の更新 |
|--------|----------|--------------|
| Create React App | 30-60秒 | 2-3秒 |
| Webpack | 20-40秒 | 1-2秒 |
| Vite | 1-2秒 | 0.1秒 |

---

## ESModules

### 簡単に言うと

**JavaScriptでファイルを分割して管理する仕組み（ES6の標準機能）**

### 詳しく説明

**なぜ必要？**
```javascript
// 昔のJavaScript（1つのファイルに全部書く）
// main.js （10000行！）
function getUserData() { ... }
function saveUserData() { ... }
function calculatePrice() { ... }
// ... 9950行 ...
// → 管理が大変！
```

**ESModulesを使うと：**
```javascript
// user.js
export function getUserData() { ... }
export function saveUserData() { ... }

// price.js
export function calculatePrice() { ... }

// main.js
import { getUserData, saveUserData } from './user.js';
import { calculatePrice } from './price.js';

// → ファイルを分けて管理できる！
```

**基本構文：**

```javascript
// エクスポート（他のファイルで使えるようにする）
export const API_URL = 'http://localhost:8080';
export function add(a, b) {
  return a + b;
}

// インポート（他のファイルから使う）
import { API_URL, add } from './constants.js';

console.log(API_URL);  // 'http://localhost:8080'
console.log(add(1, 2)); // 3
```

**デフォルトエクスポート：**
```javascript
// client.ts
const apiClient = axios.create({ ... });
export default apiClient;

// 他のファイル
import apiClient from './client';  // 好きな名前でインポート可能
```

**本棚の例え：**
```
ESModulesなし:
→ 全ての本が1つの巨大な本にまとまっている

ESModulesあり:
→ テーマごとに本が分かれている
→ 必要な本だけ取り出せる
```

---

## Promise / Promiseベース

### 簡単に言うと

**「後で結果を返すよ」という約束を表すJavaScriptのオブジェクト**

### 詳しく説明

**なぜ必要？**
```javascript
// APIからデータを取得（時間がかかる）
const data = getDataFromServer();  // ❌ 結果が返ってくるまで待てない
console.log(data);  // undefined（まだ取得できていない）
```

**Promiseを使うと：**
```javascript
// Promiseを返す
function getDataFromServer() {
  return new Promise((resolve, reject) => {
    // 時間のかかる処理
    setTimeout(() => {
      resolve({ name: '太郎', age: 25 });  // 成功時
    }, 2000);
  });
}

// 使い方1: thenで受け取る
getDataFromServer()
  .then(data => {
    console.log(data);  // { name: '太郎', age: 25 }
  })
  .catch(error => {
    console.error(error);  // エラー時
  });

// 使い方2: async/awaitで受け取る（推奨）
async function fetchData() {
  try {
    const data = await getDataFromServer();  // 結果を待つ
    console.log(data);  // { name: '太郎', age: 25 }
  } catch (error) {
    console.error(error);
  }
}
```

**Promiseの3つの状態：**
```
1. Pending（保留中）
   → まだ処理中

2. Fulfilled（成功）
   → 処理完了、結果あり

3. Rejected（失敗）
   → エラー発生
```

**レストランの注文の例え：**
```
1. 注文する（Promise作成）
   「ハンバーガーを1つください」

2. 調理中（Pending）
   「少々お待ちください」

3. 完成（Fulfilled）
   「お待たせしました！」
   → .then() で受け取る

3'. 材料切れ（Rejected）
   「申し訳ございません、売り切れです」
   → .catch() でエラー処理
```

**Promiseベースとは？**
- AxiosなどのライブラリがPromiseを返す設計のこと
- async/awaitで使いやすい

---

## インターセプター機能

### 簡単に言うと

**全てのリクエスト/レスポンスに共通処理を自動で追加する機能**

### 詳しく説明

**問題：毎回同じコードを書くのは面倒**
```javascript
// API呼び出し1
axios.get('/api/users', {
  headers: { Authorization: 'Bearer token123' }  // 認証トークン
});

// API呼び出し2
axios.get('/api/products', {
  headers: { Authorization: 'Bearer token123' }  // また書く
});

// API呼び出し3
axios.post('/api/orders', data, {
  headers: { Authorization: 'Bearer token123' }  // また書く
});

// → 全部に同じコードを書かないといけない！
```

**インターセプターを使うと：**
```javascript
// 1回だけ設定（client.ts）
axios.interceptors.request.use((config) => {
  // 全てのリクエストの前に自動実行
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// あとは普通に呼び出すだけ
axios.get('/api/users');      // 自動でトークン追加
axios.get('/api/products');   // 自動でトークン追加
axios.post('/api/orders', data);  // 自動でトークン追加
```

**仕組み：**
```
リクエスト
    ↓
【インターセプター】← ここで共通処理
    ↓
サーバーへ送信
    ↓
レスポンス
    ↓
【インターセプター】← ここで共通処理
    ↓
処理結果を受け取る
```

**具体例：**
```javascript
// リクエストインターセプター
axios.interceptors.request.use((config) => {
  console.log('送信:', config.url);
  // 認証トークンを追加
  // ローディング表示を開始
  return config;
});

// レスポンスインターセプター
axios.interceptors.response.use(
  (response) => {
    console.log('成功:', response.data);
    // ローディング表示を終了
    return response;
  },
  (error) => {
    console.error('エラー:', error.message);
    // エラーメッセージを表示
    // ローディング表示を終了
    return Promise.reject(error);
  }
);
```

**空港のセキュリティチェックの例え：**
```
インターセプターなし:
→ 各自で荷物チェック（面倒）

インターセプターあり:
→ 全員が必ず通るゲートでチェック（効率的）
```

---

## JSONパース

### 簡単に言うと

**JSON文字列をJavaScriptのオブジェクトに変換すること**

### 詳しく説明

**JSONとは？**
- JavaScript Object Notation
- データをやり取りするための文字列形式
- サーバーとの通信で使われる

**例：**
```javascript
// JSON（文字列）
const jsonString = '{"name":"太郎","age":25}';
console.log(typeof jsonString);  // "string"

// パース（変換）
const obj = JSON.parse(jsonString);
console.log(typeof obj);  // "object"
console.log(obj.name);    // "太郎"
console.log(obj.age);     // 25
```

**サーバーとの通信：**
```
1. サーバーがデータを送信
   → JSON文字列: '{"id":1,"name":"ヘアカット"}'

2. フロントエンドで受信
   → まだ文字列（使えない）

3. パース
   → JavaScriptオブジェクトに変換
   → obj.name でアクセス可能
```

**fetchの場合（手動パース必要）：**
```javascript
fetch('/api/service-menus')
  .then(response => response.json())  // ← パースが必要
  .then(data => {
    console.log(data.name);  // 使える
  });
```

**Axiosの場合（自動パース）：**
```javascript
axios.get('/api/service-menus')
  .then(response => {
    console.log(response.data.name);  // 自動でパース済み
  });
```

**逆変換（stringify）：**
```javascript
// JavaScriptオブジェクト
const obj = { name: '太郎', age: 25 };

// JSON文字列に変換
const jsonString = JSON.stringify(obj);
console.log(jsonString);  // '{"name":"太郎","age":25}'

// サーバーに送信
axios.post('/api/users', obj);  // Axiosが自動でstringify
```

**手紙の例え：**
```
JSONパース:
→ 封筒から手紙を取り出して読む

JSON.stringify:
→ 手紙を封筒に入れる
```

---

## シングルページアプリケーション（SPA）

### 簡単に言うと

**1つのHTMLページで全ての画面を表示するWebアプリケーション**

### 詳しく説明

**従来のWebサイト（マルチページアプリケーション）：**
```
トップページ (/)
↓ クリック
サーバーにリクエスト
↓
サービスメニュー一覧ページ (/service-menus) をダウンロード
↓ ページ全体が再読み込み（画面が白くなる）
↓ 表示

予約ページ (/reservations) へ移動
↓ クリック
サーバーにリクエスト
↓
予約ページをダウンロード
↓ ページ全体が再読み込み
↓ 表示
```

**SPA：**
```
最初のアクセス
↓
index.html + JavaScript をダウンロード（1回だけ）
↓
以降はJavaScriptで画面を切り替え

トップページ (/) 表示
↓ クリック
JavaScriptで画面を書き換え（ダウンロードなし）
↓
サービスメニュー一覧ページ (/service-menus) 表示
↓ クリック
JavaScriptで画面を書き換え
↓
予約ページ (/reservations) 表示

データが必要な時だけAPIで取得
```

**メリット：**
1. **ページ遷移が高速**
   - サーバーからHTMLをダウンロードしない
   - 画面が白くならない

2. **ネイティブアプリのような操作感**
   - スムーズな画面遷移
   - アニメーションが自然

3. **サーバーの負荷軽減**
   - HTMLを返さない（JSONデータだけ）
   - 1回の読み込みで済む

**デメリット：**
1. **初回読み込みが遅い**
   - 全てのJavaScriptをダウンロード

2. **SEO対策が必要**
   - 検索エンジンが読み取りにくい
   - SSR（サーバーサイドレンダリング）で対策

**本の例え：**
```
従来型:
→ 各章ごとに本を買いに行く

SPA:
→ 最初に本全体を買う
→ ページをめくるだけ（速い）
```

**有名なSPA：**
- Gmail
- Twitter/X
- Facebook
- YouTube

---

## package-lock.json

### 簡単に言うと

**インストールした全ライブラリのバージョンを正確に記録したファイル**

### 詳しく説明

**package.jsonとの違い：**

**package.json:**
```json
{
  "dependencies": {
    "axios": "^1.6.0"
  }
}
```
- `^1.6.0`: 1.6.0以上、2.0.0未満なら何でもOK
- あいまい（柔軟）

**package-lock.json:**
```json
{
  "dependencies": {
    "axios": {
      "version": "1.6.2",
      "resolved": "https://registry.npmjs.org/axios/-/axios-1.6.2.tgz",
      "dependencies": {
        "follow-redirects": {
          "version": "1.15.3"
        }
      }
    }
  }
}
```
- 正確なバージョン（1.6.2）
- 依存関係も全部記録
- 厳密

**なぜ必要？**

**問題のケース：**
```
開発者Aの環境:
axios 1.6.0 インストール
→ 動作OK

1週間後...

開発者Bの環境:
axios 1.6.2 がリリース済み
→ npm install
→ axios 1.6.2 がインストールされる
→ 動作しない！（バージョン違いでバグ）
```

**package-lock.jsonがあると：**
```
開発者A:
npm install
→ package-lock.json を見る
→ axios 1.6.0 をインストール

開発者B:
npm install
→ package-lock.json を見る
→ axios 1.6.0 をインストール（同じバージョン）
→ 動作OK！
```

**使い分け：**
```bash
# 新しいライブラリを追加
npm install axios
→ package.json に追加
→ package-lock.json も更新

# 他の人の環境で同じバージョンをインストール
npm ci  # package-lock.json を厳密に従う
```

**Gitでの管理：**
```
✅ コミットするべき:
- package.json
- package-lock.json

❌ コミットしない:
- node_modules/ （ライブラリの実体）
```

**レシピの例え：**
```
package.json:
→ 「小麦粉 適量」（あいまい）

package-lock.json:
→ 「小麦粉 250g、○○製粉、ロット番号123」（厳密）
```

---

## CI/CD

### 簡単に言うと

**コードを自動でテスト・ビルド・デプロイする仕組み**

### 詳しく説明

**CI（Continuous Integration：継続的インテグレーション）:**
```
開発者がコードをプッシュ
    ↓
自動的にテスト実行
    ↓
合格 → メインブランチにマージ
失敗 → 開発者に通知
```

**CD（Continuous Deployment：継続的デプロイ）:**
```
メインブランチにマージ
    ↓
自動的にビルド
    ↓
自動的に本番環境にデプロイ
    ↓
ユーザーが新機能を使える
```

**従来の手動デプロイ（大変）：**
```
1. コードを書く
2. 手動でテスト実行
3. テスト合格を確認
4. 手動でビルド
5. サーバーにSSH接続
6. ファイルをアップロード
7. サーバーで起動コマンド実行
8. 動作確認

→ 30分かかる
→ ミスしやすい
→ 夜中の作業
```

**CI/CDの自動デプロイ：**
```
1. コードを書く
2. Gitにプッシュ
   ↓
以下、全て自動
3. テスト実行
4. ビルド
5. デプロイ
6. 動作確認

→ 5分で完了
→ ミスなし
→ いつでもデプロイ可能
```

**人気のCI/CDツール：**
- GitHub Actions
- GitLab CI/CD
- CircleCI
- Jenkins

**GitHub Actionsの例：**
```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [ main ]  # mainブランチにプッシュしたら実行

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3  # コード取得
      - run: npm ci                # 依存関係インストール
      - run: npm test              # テスト実行
      - run: npm run build         # ビルド
      - run: deploy.sh             # デプロイ
```

**工場の例え：**
```
手動:
→ 職人が1つずつ手作業で検品・梱包・出荷

CI/CD:
→ ベルトコンベアで自動的に検品・梱包・出荷
```

**メリット：**
1. 人為的ミスがない
2. デプロイが速い
3. いつでもデプロイできる
4. バグを早期発見

---

## カスタムフック

### 簡単に言うと

**Reactのロジックを再利用可能な関数として切り出したもの**

### 詳しく説明

**Reactのフックとは？**
- `useState`, `useEffect` などのReact機能
- 関数コンポーネントで状態管理や副作用を扱う

**問題：同じロジックを複数のコンポーネントで使いたい**
```typescript
// ユーザー一覧ページ
function UserListPage() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  useEffect(() => {
    setLoading(true);
    fetch('/api/users')
      .then(res => res.json())
      .then(data => setUsers(data))
      .catch(err => setError(err))
      .finally(() => setLoading(false));
  }, []);

  // ... 表示
}

// 商品一覧ページ
function ProductListPage() {
  const [products, setProducts] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState(null);

  useEffect(() => {
    setLoading(true);
    fetch('/api/products')
      .then(res => res.json())
      .then(data => setProducts(data))
      .catch(err => setError(err))
      .finally(() => setLoading(false));
  }, []);

  // ... 表示（同じパターン）
}

// → 同じコードを繰り返し書いている！
```

**カスタムフックで共通化：**
```typescript
// hooks/useFetch.ts
function useFetch<T>(url: string) {
  const [data, setData] = useState<T | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    setLoading(true);
    fetch(url)
      .then(res => res.json())
      .then(data => setData(data))
      .catch(err => setError(err))
      .finally(() => setLoading(false));
  }, [url]);

  return { data, loading, error };
}

// 使う側
function UserListPage() {
  const { data: users, loading, error } = useFetch<User[]>('/api/users');

  if (loading) return <div>読み込み中...</div>;
  if (error) return <div>エラー: {error.message}</div>;

  return <ul>{users?.map(user => <li>{user.name}</li>)}</ul>;
}

function ProductListPage() {
  const { data: products, loading, error } = useFetch<Product[]>('/api/products');

  if (loading) return <div>読み込み中...</div>;
  if (error) return <div>エラー: {error.message}</div>;

  return <ul>{products?.map(p => <li>{p.name}</li>)}</ul>;
}
```

**命名規則：**
- `use` で始める（例: `useFetch`, `useAuth`, `useForm`）
- Reactがカスタムフックと認識する

**よくあるカスタムフック：**
```typescript
// 認証状態を管理
function useAuth() {
  const [user, setUser] = useState(null);
  const [isAuthenticated, setIsAuthenticated] = useState(false);

  const login = async (email, password) => { ... };
  const logout = () => { ... };

  return { user, isAuthenticated, login, logout };
}

// フォームの状態管理
function useForm(initialValues) {
  const [values, setValues] = useState(initialValues);

  const handleChange = (e) => { ... };
  const reset = () => { ... };

  return { values, handleChange, reset };
}
```

**調理器具の例え：**
```
カスタムフックなし:
→ 毎回、材料を切る・炒める・味付けを手作業

カスタムフックあり:
→ フードプロセッサー（一度作れば再利用）
```

---

## ユーティリティ関数

### 簡単に言うと

**プロジェクト全体で使う便利な関数をまとめたもの**

### 詳しく説明

**例：日付のフォーマット**
```typescript
// utils/formatDate.ts
export function formatDate(date: string): string {
  const d = new Date(date);
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${year}年${month}月${day}日`;
}

// 使う側
import { formatDate } from '../utils/formatDate';

// "2026-01-10T10:00:00" → "2026年01月10日"
const formatted = formatDate("2026-01-10T10:00:00");
```

**よくあるユーティリティ関数：**

```typescript
// utils/validation.ts
export function isValidEmail(email: string): boolean {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return regex.test(email);
}

export function isValidPassword(password: string): boolean {
  return password.length >= 8;
}

// utils/currency.ts
export function formatCurrency(amount: number): string {
  return `¥${amount.toLocaleString()}`;
}
// 3000 → "¥3,000"

// utils/storage.ts
export function saveToLocalStorage(key: string, value: any): void {
  localStorage.setItem(key, JSON.stringify(value));
}

export function getFromLocalStorage<T>(key: string): T | null {
  const item = localStorage.getItem(key);
  return item ? JSON.parse(item) : null;
}

// utils/array.ts
export function groupBy<T>(array: T[], key: keyof T): Record<string, T[]> {
  return array.reduce((result, item) => {
    const group = String(item[key]);
    if (!result[group]) result[group] = [];
    result[group].push(item);
    return result;
  }, {} as Record<string, T[]>);
}
```

**配置場所：**
```
src/
└── utils/
    ├── formatDate.ts
    ├── validation.ts
    ├── currency.ts
    ├── storage.ts
    └── array.ts
```

**工具箱の例え：**
```
ユーティリティ関数 = よく使う工具

ドライバー → formatDate
ハンマー → validation
ペンチ → currency

必要な時にすぐ取り出せる
```

**コンポーネントとの違い：**
- コンポーネント: UIを返す（JSX）
- ユーティリティ関数: 値を返す（計算・変換）

---

## プロキシ機能

### 簡単に言うと

**クライアントとサーバーの間に入って、リクエストを中継する仕組み**

### 詳しく説明

**なぜ必要？**

**CORS問題：**
```
ブラウザのセキュリティ制限
→ 異なるオリジン（ドメイン・ポート）への通信をブロック

フロントエンド: http://localhost:5173
バックエンド:   http://localhost:8080
→ 異なるポート → ブラウザがブロック！
```

**プロキシの仕組み：**
```
【プロキシなし（エラー）】
ブラウザ (5173)
    ↓ XMLHttpRequest
バックエンド (8080)
    ↓
❌ CORS Error

【プロキシあり（成功）】
ブラウザ (5173)
    ↓ /api/service-menus
Viteサーバー (5173) ← 同じオリジン（OK）
    ↓ プロキシ転送
バックエンド (8080) ← サーバー間通信（CORS制限なし）
    ↓ レスポンス
Viteサーバー (5173)
    ↓
ブラウザ (5173)
    ↓
✅ 成功
```

**Viteのプロキシ設定：**
```typescript
// vite.config.ts
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
});
```

**動作：**
```typescript
// コードではこう書く
axios.get('/api/service-menus')

// 実際には
// 1. Viteサーバーにリクエスト: http://localhost:5173/api/service-menus
// 2. Viteが転送: http://localhost:8080/api/service-menus
// 3. レスポンスを返す
```

**本番環境では？**
```
Nginx（リバースプロキシ）を使用

https://example.com/
    ↓
Nginx
    ├─ /api/*  → バックエンド (8080)
    └─ /*      → フロントエンド (静的ファイル)
```

**受付の例え：**
```
プロキシなし:
→ 直接、担当部署に電話（繋がらない）

プロキシあり:
→ 受付に電話 → 受付が担当部署に転送（繋がる）
```

---

## バンドル

### 簡単に言うと

**複数のファイルを1つ（または少数）のファイルにまとめること**

### 詳しく説明

**なぜ必要？**

**開発時：**
```
src/
├── App.tsx
├── Header.tsx
├── Footer.tsx
├── UserList.tsx
├── UserItem.tsx
├── api/
│   ├── client.ts
│   └── userApi.ts
├── types/
│   └── index.ts
└── utils/
    ├── formatDate.ts
    └── validation.ts

→ 10個以上のファイル
```

**問題：**
```
ブラウザが10個のファイルをダウンロード
→ 10回のHTTPリクエスト
→ 遅い
```

**バンドル後：**
```
dist/
├── index.html
└── assets/
    ├── index-abc123.js   ← 全JavaScriptがまとまっている
    └── index-def456.css  ← 全CSSがまとまっている

→ 2回のHTTPリクエスト
→ 速い
```

**バンドルツール：**
1. **Webpack**（従来の主流）
2. **Vite**（開発は高速、本番ビルドはRollup使用）
3. **Rollup**
4. **esbuild**

**バンドル処理の内容：**
```typescript
// 1. トランスパイル（変換）
TypeScript → JavaScript
最新JavaScript → 古いブラウザ対応JavaScript

// 2. ミニファイ（圧縮）
変数名を短縮、空白削除、コメント削除
元: function calculateTotalPrice(items) { return items.reduce(...) }
後: function c(a){return a.reduce(...)}

// 3. 最適化
使われていないコード削除（Tree Shaking）
重複コード削除

// 4. 結合
全ファイルを1つにまとめる
```

**ファイルサイズの比較：**
```
開発時（バンドル前）:
├── App.tsx (2KB)
├── Header.tsx (1KB)
├── Footer.tsx (1KB)
├── UserList.tsx (3KB)
└── ... (50KB)
合計: 57KB

本番（バンドル後）:
└── index-abc123.js
圧縮前: 57KB
圧縮後: 18KB（68%削減）
```

**本の例え：**
```
バンドル前:
→ 各章が別々のファイル（バラバラ）

バンドル後:
→ 1冊の本にまとまっている（持ち運びやすい）
```

**Viteのビルドコマンド：**
```bash
npm run build

# 出力
dist/
├── index.html
└── assets/
    ├── index-a1b2c3.js  (圧縮済み)
    └── index-d4e5f6.css (圧縮済み)
```

---

## Nginx

### 簡単に言うと

**Webサーバー・リバースプロキシとして使われる高性能なソフトウェア**

### 詳しく説明

**何ができる？**

**1. Webサーバー（静的ファイル配信）**
```
ブラウザ → Nginx → HTML/CSS/JSファイルを返す
```

**2. リバースプロキシ（振り分け）**
```
ブラウザ
    ↓
Nginx
    ├─ /api/*  → バックエンド (Spring Boot)
    └─ /*      → フロントエンド (静的ファイル)
```

**3. ロードバランサー（負荷分散）**
```
ブラウザ
    ↓
Nginx
    ├─ サーバー1 (50%)
    └─ サーバー2 (50%)
```

**なぜNginx？**

**Apache vs Nginx:**
| 項目 | Apache | Nginx |
|------|--------|-------|
| 処理方式 | プロセスベース | イベント駆動 |
| メモリ使用量 | 多い | 少ない |
| 同時接続数 | 少ない | 多い |
| 静的ファイル | 普通 | 高速 |
| 設定 | 複雑 | シンプル |

**Nginxの設定例：**

```nginx
# /etc/nginx/nginx.conf

server {
    listen 80;
    server_name example.com;

    # フロントエンド（静的ファイル）
    location / {
        root /var/www/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # バックエンド（プロキシ）
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

**解説：**
```nginx
listen 80;
→ ポート80で待ち受け

location / {
→ ルート（/）へのアクセス

root /var/www/frontend/dist;
→ 静的ファイルの場所

try_files $uri $uri/ /index.html;
→ ファイルがなければindex.htmlを返す（SPAのため）

location /api/ {
→ /api/へのアクセス

proxy_pass http://localhost:8080;
→ バックエンド（8080）に転送
```

**実際の動作：**
```
https://example.com/
→ Nginx → /var/www/frontend/dist/index.html

https://example.com/service-menus
→ Nginx → /var/www/frontend/dist/index.html（SPA）

https://example.com/api/service-menus
→ Nginx → http://localhost:8080/api/service-menus（プロキシ）
```

**交通整理の例え：**
```
Nginx = 交通整理の警察官

/api/への車 → バックエンド行きの道へ誘導
/への車 → フロントエンド行きの道へ誘導
```

**他の用途：**
1. **HTTPS対応（SSL/TLS）**
   ```nginx
   listen 443 ssl;
   ssl_certificate /path/to/cert.pem;
   ssl_certificate_key /path/to/key.pem;
   ```

2. **キャッシュ**
   ```nginx
   location ~* \.(jpg|jpeg|png|gif|css|js)$ {
       expires 30d;
   }
   ```

3. **圧縮**
   ```nginx
   gzip on;
   gzip_types text/css application/javascript;
   ```

**人気のWebサーバー：**
1. **Nginx** - 高性能、リバースプロキシに強い
2. **Apache** - 老舗、機能豊富
3. **Caddy** - 自動HTTPS、設定簡単

---

## まとめ

### 重要度順

**🔥 最重要（必ず理解）:**
1. **Promise** - 非同期処理の基本
2. **SPA** - モダンなWebアプリの仕組み
3. **JSONパース** - サーバーとの通信
4. **プロキシ** - CORS問題の解決

**⭐ 重要（理解推奨）:**
5. **仮想DOM** - Reactの高速化の仕組み
6. **ESModules** - ファイル分割の仕組み
7. **インターセプター** - 共通処理の実装
8. **カスタムフック** - ロジックの再利用

**📚 知っておくと良い:**
9. **Vite** - 開発ツールの選択肢
10. **package-lock.json** - バージョン管理
11. **CI/CD** - 自動化の仕組み
12. **ユーティリティ関数** - コード整理
13. **バンドル** - 本番ビルド
14. **Nginx** - 本番環境の構成

---

## 学習の順番

**ステップ1: 基礎**
1. Promise（非同期処理）
2. JSONパース
3. ESModules

**ステップ2: React**
4. 仮想DOM
5. SPA
6. カスタムフック

**ステップ3: 開発環境**
7. Vite
8. プロキシ
9. package-lock.json

**ステップ4: 実践**
10. インターセプター
11. ユーティリティ関数

**ステップ5: 本番環境**
12. バンドル
13. CI/CD
14. Nginx

---

この用語集を読んで、わからない部分があれば遠慮なく質問してください！
