# デプロイ手順書

このドキュメントは、予約システムのデプロイ手順と、発生した問題・解決策をまとめたものです。

---

## 📋 目次

1. [システム構成](#1-システム構成)
2. [事前準備](#2-事前準備)
3. [バックエンド（EC2）デプロイ](#3-バックエンドec2デプロイ)
4. [フロントエンド（Vercel）デプロイ](#4-フロントエンドvercelデプロイ)
5. [Gmail SMTP設定（メール認証）](#5-gmail-smtp設定メール認証)
6. [発生した問題と解決策](#6-発生した問題と解決策)
7. [動作確認](#7-動作確認)
8. [運用・管理コマンド](#8-運用管理コマンド)
9. [トラブルシューティング](#9-トラブルシューティング)

---

## 1. システム構成

### デプロイ後の構成図

```
┌─────────────────┐     HTTPS      ┌─────────────────┐
│                 │ ─────────────→ │                 │
│   ブラウザ       │                │   Vercel        │
│                 │ ←───────────── │  (フロントエンド) │
└─────────────────┘                └────────┬────────┘
                                            │
                                            │ Vercelプロキシ
                                            │ (HTTP)
                                            ▼
                                   ┌─────────────────┐
                                   │                 │
                                   │   EC2           │
                                   │  (バックエンド)  │
                                   │   :8080         │
                                   └────────┬────────┘
                                            │
                                            ▼
                                   ┌─────────────────┐
                                   │   PostgreSQL    │
                                   │   (Docker)      │
                                   └─────────────────┘
```

### 各コンポーネント

| コンポーネント | 技術スタック | URL |
|---------------|-------------|-----|
| フロントエンド | React + Vite + TypeScript | https://reservation-system-liart.vercel.app/ |
| バックエンド | Spring Boot 3.4 | http://18.183.192.121:8080 |
| データベース | PostgreSQL 16 (Docker) | EC2内部 |

---

## 2. 事前準備

### 2.1 必要なもの

- **EC2インスタンス**: Amazon Linux 2023
- **SSH秘密鍵**: `~/.ssh/nagi5417-key.pem`
- **Vercelアカウント**: GitHubリポジトリと連携済み
- **GitHubリポジトリ**: https://github.com/nagi5417/reservation-system

### 2.2 EC2のセキュリティグループ設定

以下のインバウンドルールを設定：

| タイプ | ポート | ソース | 用途 |
|-------|-------|--------|------|
| SSH | 22 | マイIP | SSH接続 |
| カスタムTCP | 8080 | 0.0.0.0/0 | APIアクセス |

### 2.3 EC2にDockerとJavaをインストール

```bash
# EC2に接続
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121

# Dockerインストール
sudo yum update -y
sudo yum install -y docker
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker ec2-user

# Docker Composeインストール
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Javaインストール
sudo yum install -y java-17-amazon-corretto
```

---

## 3. バックエンド（EC2）デプロイ

### 3.1 JARファイルのビルド

ローカルマシンで実行：

```bash
cd /Users/yanagitanaoki/Documents/ReservationSystem/reservation

# クリーンビルド
./gradlew clean bootJar
```

ビルド成功後、`build/libs/reservation-0.0.1-SNAPSHOT.jar` が生成される。

### 3.2 JARファイルをEC2に転送

```bash
scp -i ~/.ssh/nagi5417-key.pem \
  ./build/libs/reservation-0.0.1-SNAPSHOT.jar \
  ec2-user@18.183.192.121:~/reservation.jar
```

### 3.3 EC2でアプリケーションディレクトリを作成

```bash
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121

# アプリケーションディレクトリ作成
sudo mkdir -p /opt/reservation
sudo cp ~/reservation.jar /opt/reservation/app.jar
```

### 3.4 systemdサービスファイルの作成

```bash
sudo vi /etc/systemd/system/reservation.service
```

内容：
```ini
[Unit]
Description=reservation backend
After=network.target

[Service]
Type=simple
User=ec2-user
WorkingDirectory=/opt/reservation
ExecStart=/usr/bin/java -jar /opt/reservation/app.jar
Restart=always
RestartSec=10

# 環境変数
Environment=SPRING_PROFILES_ACTIVE=prod
Environment=DB_HOST=localhost
Environment=DB_PORT=5432
Environment=DB_NAME=reservation_db
Environment=DB_USERNAME=reservation_user
Environment=DB_PASSWORD=reservation_pass
Environment=APP_BASE_URL=https://reservation-system-liart.vercel.app

[Install]
WantedBy=multi-user.target
```

### 3.5 PostgreSQLコンテナの起動

```bash
# docker-compose.ymlを作成（またはローカルからコピー）
cd /opt/reservation

# PostgreSQL起動
docker-compose up -d
```

### 3.6 サービスの起動

```bash
# サービスを有効化・起動
sudo systemctl daemon-reload
sudo systemctl enable reservation
sudo systemctl start reservation

# 状態確認
sudo systemctl status reservation
```

### 3.7 デプロイ更新時の手順

```bash
# ローカルでビルド
./gradlew clean bootJar

# EC2に転送
scp -i ~/.ssh/nagi5417-key.pem \
  ./build/libs/reservation-0.0.1-SNAPSHOT.jar \
  ec2-user@18.183.192.121:~/reservation.jar

# EC2でコピー＆再起動
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo cp ~/reservation.jar /opt/reservation/app.jar && sudo systemctl restart reservation"
```

---

## 4. フロントエンド（Vercel）デプロイ

### 4.1 Vercelプロジェクトの設定

1. [Vercel](https://vercel.com)にログイン
2. 「New Project」→ GitHubリポジトリを選択
3. 設定：
   - **Framework Preset**: Vite
   - **Root Directory**: `reservation-frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

### 4.2 vercel.jsonの設定（重要）

`reservation-frontend/vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://18.183.192.121:8080/api/:path*"
    },
    {
      "source": "/oauth2/:path*",
      "destination": "http://18.183.192.121:8080/oauth2/:path*"
    },
    {
      "source": "/login/oauth2/:path*",
      "destination": "http://18.183.192.121:8080/login/oauth2/:path*"
    },
    {
      "source": "/((?!api|oauth2|assets|_next).*)",
      "destination": "/index.html"
    }
  ]
}
```

**この設定が重要な理由：**
- Vercel（HTTPS）からEC2（HTTP）への直接リクエストは「Mixed Content」エラーでブロックされる
- Vercelのrewrite機能を使い、Vercelがプロキシとしてリクエストを中継することで解決
- **最後のルール**: SPAルーティングのフォールバック設定。`/login`などのフロントエンドルートに直接アクセスした際に404にならないようにする

### 4.3 デプロイの実行

GitHubにプッシュすると自動でデプロイされる：

```bash
git add .
git commit -m "deploy: Vercel設定追加"
git push origin main
```

---

## 5. Gmail SMTP設定（メール認証）

本番環境でメール認証機能を動作させるには、Gmail SMTPの設定が必要です。

### 5.1 Googleアプリパスワードの取得

1. [Googleアカウント設定](https://myaccount.google.com/)にアクセス
2. **セキュリティ** → **2段階認証プロセス**を有効化（まだの場合）
3. **セキュリティ** → **アプリパスワード**に移動
4. アプリを選択：「メール」、デバイスを選択：「その他」→ 「予約システム」と入力
5. **生成**をクリック
6. 表示された**16文字のパスワード**をメモ（スペースは除く）

### 5.2 EC2環境変数の設定

EC2の環境ファイル（`/opt/reservation/app.env`）に以下を追加：

```bash
# Gmail SMTP設定
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=xxxx xxxx xxxx xxxx  # アプリパスワード（スペースなし）
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true
MAIL_FROM=your-email@gmail.com
```

### 5.3 設定の反映

```bash
# サービス再起動
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo systemctl restart reservation"
```

### 5.4 注意事項

- **APP_BASE_URL**の末尾にスラッシュを付けない
  - ✅ 正しい: `https://reservation-system-liart.vercel.app`
  - ❌ 間違い: `https://reservation-system-liart.vercel.app/`
  - 末尾にスラッシュがあると、メール認証リンクのURLが`//login`（二重スラッシュ）になり404エラーになる

- **Gmail SMTPの制限**
  - 送信元アドレスは認証に使用したGmailアドレスのみ使用可能
  - 別のアドレスを送信元にしたい場合はAmazon SESやSendGridを検討

---

## 6. 発生した問題と解決策

### 6.1 Mixed Content エラー

#### 症状
ブラウザのコンソールに以下のエラーが表示される：

```
Mixed Content: The page at 'https://reservation-system-liart.vercel.app/'
was loaded over HTTPS, but requested an insecure XMLHttpRequest endpoint
'http://18.183.192.121:8080/api/slots'. This request has been blocked;
the content must be served over HTTPS.
```

#### 原因
- Vercel（HTTPS）からEC2（HTTP）への直接リクエスト
- ブラウザのセキュリティポリシーでブロックされる

#### 解決策
**Vercelプロキシを設定**

`vercel.json`でrewritesを設定し、VercelがEC2へのリクエストを代理で送信するようにした。

```
ブラウザ → Vercel (HTTPS) → Vercelプロキシ → EC2 (HTTP)
                ↑
          ここでHTTPSが終端されるのでMixed Contentにならない
```

### 6.2 CORS エラー

#### 症状
```
Access to XMLHttpRequest at 'http://18.183.192.121:8080/api/slots'
from origin 'https://reservation-system-liart.vercel.app'
has been blocked by CORS policy
```

#### 原因
- バックエンドのCORS設定にVercelドメインが含まれていなかった

#### 解決策
`SecurityConfig.java`のCORS設定にVercelドメインを追加：

```java
@Bean
public CorsConfigurationSource corsConfigurationSource() {
    CorsConfiguration configuration = new CorsConfiguration();
    configuration.setAllowedOrigins(Arrays.asList(
        "http://localhost:5173",
        "https://reservation-system-liart.vercel.app"  // 追加
    ));
    configuration.setAllowedMethods(Arrays.asList("GET", "POST", "PUT", "DELETE", "OPTIONS"));
    configuration.setAllowedHeaders(Arrays.asList("*"));
    configuration.setAllowCredentials(true);
    // ...
}
```

### 6.3 API 401エラー（認証不要APIなのに認証エラー）

#### 症状
`/api/service-menus` が401 Unauthorizedを返す（本来は認証不要のはず）

#### 原因
`SecurityConfig.java`のエンドポイントパスが間違っていた：

```java
// 間違い
.requestMatchers(HttpMethod.GET, "/api/menus/**").permitAll()

// 正しい（実際のエンドポイント）
.requestMatchers(HttpMethod.GET, "/api/service-menus/**").permitAll()
```

#### 解決策
`SecurityConfig.java`のパスを実際のエンドポイントに修正：

```java
// メニュー関連（GETは公開、その他はスタッフのみ）
.requestMatchers(HttpMethod.GET, "/api/service-menus/**").permitAll()
.requestMatchers("/api/service-menus/**").hasRole("STAFF")
```

### 6.4 GlobalExceptionHandlerが500を返す

#### 症状
認証エラーや404エラーがすべて500 Internal Server Errorとして返される

#### 原因
`GlobalExceptionHandler`にSpring Security例外のハンドラーがなかった

#### 解決策
以下のハンドラーを追加：

```java
// 403 Forbidden
@ExceptionHandler(AccessDeniedException.class)
public ResponseEntity<Map<String, Object>> handleAccessDeniedException(AccessDeniedException ex) {
    // ...
    return ResponseEntity.status(HttpStatus.FORBIDDEN).body(errorResponse);
}

// 401 Unauthorized
@ExceptionHandler(AuthenticationException.class)
public ResponseEntity<Map<String, Object>> handleAuthenticationException(AuthenticationException ex) {
    // ...
    return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(errorResponse);
}

// 404 Not Found
@ExceptionHandler(NoHandlerFoundException.class)
public ResponseEntity<Map<String, Object>> handleNoHandlerFoundException(NoHandlerFoundException ex) {
    // ...
    return ResponseEntity.status(HttpStatus.NOT_FOUND).body(errorResponse);
}
```

また、`application.yml`に以下を追加：

```yaml
spring:
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
```

### 6.5 SPAルーティングで404エラー

#### 症状
メール認証後のリダイレクト先（`/login?verified=true`）で404エラーが発生

```
404: NOT_FOUND
Code: NOT_FOUND
```

#### 原因
Vercelでは、`/login`のようなフロントエンドルートに直接アクセスすると、対応するファイルがないため404を返す

#### 解決策
`vercel.json`にSPAフォールバック設定を追加：

```json
{
  "rewrites": [
    // ... 他のrewrite設定 ...
    {
      "source": "/((?!api|oauth2|assets|_next).*)",
      "destination": "/index.html"
    }
  ]
}
```

### 6.6 メール認証リンクで404エラー（二重スラッシュ）

#### 症状
メール認証リンクをクリックすると404エラー。URLが`//login`（二重スラッシュ）になっている

```
https://reservation-system-liart.vercel.app//login?verified=true
```

#### 原因
`APP_BASE_URL`環境変数の末尾にスラッシュがある

```
# 間違い
APP_BASE_URL=https://reservation-system-liart.vercel.app/

# 正しい
APP_BASE_URL=https://reservation-system-liart.vercel.app
```

#### 解決策
EC2の環境ファイルから末尾のスラッシュを削除：

```bash
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sed -i 's|APP_BASE_URL=https://reservation-system-liart.vercel.app/|APP_BASE_URL=https://reservation-system-liart.vercel.app|' /opt/reservation/app.env"

# サービス再起動
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo systemctl restart reservation"
```

---

## 7. 動作確認

### 7.1 バックエンドの確認

```bash
# EC2サービス状態
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo systemctl status reservation"

# APIエンドポイント確認
curl -s -w "\nHTTP Status: %{http_code}\n" http://18.183.192.121:8080/api/slots
curl -s -w "\nHTTP Status: %{http_code}\n" http://18.183.192.121:8080/api/service-menus
```

期待される結果：
- ステータス: 200 OK
- レスポンス: `[]`（データがない場合）または JSON配列

### 7.2 CORSの確認

```bash
curl -s -I -X OPTIONS \
  -H "Origin: https://reservation-system-liart.vercel.app" \
  -H "Access-Control-Request-Method: GET" \
  -H "Access-Control-Request-Headers: Content-Type" \
  http://18.183.192.121:8080/api/slots
```

期待されるヘッダー：
```
Access-Control-Allow-Origin: https://reservation-system-liart.vercel.app
Access-Control-Allow-Methods: GET,POST,PUT,DELETE,OPTIONS
Access-Control-Allow-Credentials: true
```

### 7.3 フロントエンドの確認

1. https://reservation-system-liart.vercel.app/ にアクセス
2. ブラウザの開発者ツール（F12）→ Consoleタブを確認
3. エラーがなければOK

### 7.4 メール認証の確認

1. 新規ユーザー登録を実行
2. 登録したメールアドレスに認証メールが届くか確認
3. メール内のリンクをクリック
4. ログインページにリダイレクトされ、「メール認証が完了しました」と表示されればOK

---

## 8. 運用・管理コマンド

### 8.1 ユーザー一覧の確認

```bash
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "docker exec reservation-postgres psql -U reservation -d reservation \
  -c \"SELECT id, email, name, role, email_verified FROM users;\""
```

### 8.2 ユーザーのロール変更（STAFFに昇格）

```bash
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "docker exec reservation-postgres psql -U reservation -d reservation \
  -c \"UPDATE users SET role = 'STAFF' WHERE email = 'user@example.com';\""
```

### 8.3 テストユーザーの削除

```bash
# 関連データを含めて削除
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "docker exec reservation-postgres psql -U reservation -d reservation \
  -c \"DELETE FROM email_verification_tokens WHERE user_id = (SELECT id FROM users WHERE email = 'user@example.com'); \
       DELETE FROM users WHERE email = 'user@example.com';\""
```

### 8.4 サービスのログ確認

```bash
# リアルタイムログ
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo journalctl -u reservation -f"

# 最近のログ（5分以内）
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo journalctl -u reservation --since '5 minutes ago'"
```

### 8.5 環境変数の確認・編集

```bash
# 確認
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "cat /opt/reservation/app.env"

# 編集
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo vi /opt/reservation/app.env"

# 編集後は再起動が必要
ssh -i ~/.ssh/nagi5417-key.pem ec2-user@18.183.192.121 \
  "sudo systemctl restart reservation"
```

---

## 9. トラブルシューティング

### 9.1 EC2に接続できない

```bash
# SSHタイムアウト
ssh: connect to host 18.183.192.121 port 22: Operation timed out
```

**確認事項：**
1. EC2インスタンスが起動しているか
2. セキュリティグループでポート22が開いているか
3. 秘密鍵のパーミッション（`chmod 400 ~/.ssh/nagi5417-key.pem`）

### 9.2 ポート8080にアクセスできない

```bash
curl: (7) Failed to connect to 18.183.192.121 port 8080
```

**確認事項：**
1. セキュリティグループでポート8080が開いているか
2. サービスが起動しているか（`sudo systemctl status reservation`）
3. ファイアウォール設定（`sudo iptables -L`）

### 9.3 アプリケーションが起動しない

```bash
sudo journalctl -u reservation -f
```

**よくあるエラー：**
- データベース接続エラー → PostgreSQLコンテナが起動しているか確認
- ポート競合 → `lsof -i :8080`で確認
- メモリ不足 → EC2インスタンスタイプを確認

### 9.4 Vercelデプロイが失敗する

**確認事項：**
1. Vercelダッシュボードでビルドログを確認
2. `package.json`のビルドスクリプトが正しいか
3. `vercel.json`の構文エラーがないか

### 9.5 フロントエンドでAPIエラー

ブラウザの開発者ツールで確認：

| エラー | 原因 | 解決策 |
|-------|------|--------|
| Mixed Content | HTTPS→HTTP | vercel.jsonのrewritesを確認 |
| CORS | オリジン未許可 | SecurityConfigのCORS設定を確認 |
| 401 Unauthorized | 認証エラー | SecurityConfigのpermitAll()を確認 |
| 404 Not Found | パス不一致 | エンドポイントのパスを確認 |
| 500 Internal Server Error | サーバーエラー | EC2のログを確認 |

---

## 📝 チェックリスト

### デプロイ前

- [ ] ローカルで動作確認済み
- [ ] 環境変数が設定されている
- [ ] セキュリティグループが設定されている
- [ ] CORSにVercelドメインが追加されている

### デプロイ後

- [ ] EC2サービスが起動している
- [ ] APIエンドポイントが応答する
- [ ] CORSヘッダーが返される
- [ ] Vercelサイトでエラーが出ない
- [ ] ログイン・予約機能が動作する

---

## 🔗 関連ドキュメント

- [Bashコマンド解説.md](./Bashコマンド解説.md) - デプロイで使用するコマンドの詳細
- [セキュリティ設計書.md](./セキュリティ設計書.md) - 認証・認可の設定
- [システム構成・アーキテクチャ設計書.md](./システム構成・アーキテクチャ設計書.md) - 全体構成

---

最終更新: 2026年1月27日（Gmail SMTP設定、運用コマンド追加）
